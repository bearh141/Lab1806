apiVersion: v1
kind: Pod
metadata:
  name: pod-signed
  namespace: demo
spec:
  containers:
  - name: api
    image: ghcr.io/bearh141/w10-api-141:0.0.2
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
    securityContext:
      runAsUser: 1000
      runAsNonRoot: true
