apiVersion: v1
kind: Pod
metadata:
  name: test-pod-latest
  namespace: demo
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
      requests:
        cpu: 50m
        memory: 64Mi
    securityContext:
      runAsNonRoot: true
      runAsUser: 1000
